# Security

Do not open a public issue containing API keys, image payloads, task identifiers, authorization records, or browser session data. Report a suspected vulnerability privately through GitHub's security advisory interface.

The example defaults to an injected fixture client. Live use must keep credentials outside model context and source control, restrict target origins, and preserve the local call budget.

