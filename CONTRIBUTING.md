# Contributing

Run `pytest -q` and `python scripts/smoke.py` before opening a pull request. New behavior must include offline tests and must retain strict schemas, explicit authorization references, origin allowlists, bounded calls, and redacted errors.

